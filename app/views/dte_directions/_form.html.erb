<%= form_for(dte_direction) do |f| %>
  <% if dte_direction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dte_direction.errors.count, "error") %> prohibited this dte_direction from being saved:</h2>

      <ul>
      <% dte_direction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :direction %>
    <%= f.text_field :direction %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :direction_id %>
    <%= f.text_field :direction_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
